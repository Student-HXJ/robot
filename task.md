### 需求说明：玩家自动寻路与攻击逻辑（基于区域划分）

---

#### 1. 总体描述
本系统控制玩家角色在**全局检测框**内自主移动，实时检测并攻击怪物。核心包含：
- **检测框动态调整**：根据玩家位置自适应全局检测框坐标。
- **攻击逻辑**：优先攻击横向距离最近的怪物。
- **移动策略**：将检测框横向五等分，玩家在非攻击状态下按固定方向往复巡逻，触达边界区域（最左/最右块）立即转向；攻击状态下主动追踪目标怪物。

---

#### 2. 输入与定义
- **全局检测框**（矩形区域）：游戏世界中的可活动范围，坐标由系统动态维护。
- **玩家检测框**（矩形）：代表玩家当前位置与尺寸。
- **怪物检测框列表**：每个怪物包含坐标和尺寸，实时刷新。
- **关键参数**：
  - `玩家上边界与全局框上边界间距 = 30 像素`（固定）。
  - `攻击触发距离 = 100 像素`（横向距离，即两矩形中心或边界 X 轴差值的绝对值）。
  - `区域划分数量 = 5`，将全局检测框的宽度均分为 5 个等宽区块，从左至右编号为 **区块 1～5**。
  - **区块 1 和 5 为禁区**（玩家不可进入，仅可触碰其内侧边界）。

---

#### 3. 检测框动态调整（每帧执行）
- **条件**：若检测到玩家（玩家检测框有效）。
  - 计算玩家检测框的上边界 `player_top`。
  - 设置全局检测框的上边界 = `player_top - 30`。
  - 保持全局检测框的高度不变，下边界随之移动（即整体平移）。
- **条件**：若未检测到玩家。
  - 保持上一次调整后的全局检测框坐标不变，直到玩家重新出现。

---

#### 4. 玩家移动策略（巡逻与追踪）
##### 4.1 基础巡逻模式（无攻击目标时）
- 玩家有一个**当前移动方向**（左或右），初始可设定为向右。
- 玩家持续沿当前方向移动，直到**触达区块边界**：
  - 若向左移动，当玩家的检测框左边界**接触或越过区块 1 的右边界**（即进入禁区前）时，立即将方向切换为向右。
  - 若向右移动，当玩家的检测框右边界**接触或越过区块 5 的左边界**时，立即将方向切换为向左。
- 切换方向后，继续沿新方向移动，循环上述过程。
- **禁区处理**：一旦玩家意外进入区块 1 或 5（如因惯性或追赶怪物），需**立即反向移动**，直至完全离开禁区并到达对面禁区边界时，再次转向（即形成完整往返）。

##### 4.2 攻击追踪模式（有可攻击怪物时）
- **攻击目标选择**：实时计算玩家与所有怪物检测框的**横向距离**（取绝对值），选择距离最小的怪物作为目标。
- **优先级**：攻击追踪模式**高于**巡逻模式。一旦满足以下任一条件，即切换到追踪模式：
  - 目标怪物与玩家的横向距离 ≤ 100 像素 → **发动攻击**（持续攻击直到怪物消失或距离超出）。
  - 目标怪物与玩家的横向距离 > 100 像素，但怪物仍在全局检测框内 → 玩家**主动向该怪物水平移动**（忽略当前巡逻方向），直到进入攻击距离并攻击。
- **退出追踪条件**：
  - 目标怪物被消灭（从列表中移除）。
  - 目标怪物移出全局检测框（不再可检测）。
  - 当上述情况发生时，立即恢复巡逻模式，并保持退出时的位置和朝向（但需重新按巡逻规则判断方向）。

---

#### 5. 逻辑优先级与状态切换（每帧更新）
1. **检测框调整** → 2. **怪物列表更新** → 3. **判断是否存在可攻击怪物**（在检测框内且距离<100）。
   - 若存在：进入**攻击追踪模式**，攻击最近者并跟踪。
   - 若不存在但存在距离>100且在检测框内的怪物：仍进入**追踪模式**，但仅跟踪不攻击（一旦距离≤100则攻击）。
   - 若不存在任何怪物在检测框内：进入**巡逻模式**。
4. 在追踪模式下，若目标消失或移出检测框，立即切回巡逻模式。

---

#### 6. 特殊说明
- 所有距离计算仅考虑**水平方向（X轴）**，以检测框中心或边界为基准（建议统一使用中心点）。
- 玩家移动速度恒定，不受方向切换影响。
- 攻击动作视为瞬时或持续，具体效果（如伤害、怪物死亡）由外部系统处理，本逻辑仅负责触发攻击行为。
- 本需求完全替换原有的随机移动和寻路逻辑，不再保留旧逻辑。

---

#### 7. 示例流程
1. 玩家初始在区块 3，方向向右。
2. 向右移动，经过区块 4，到达区块 5 左边界 → 转向向左。
3. 向左移动，经过区块 4、3、2，到达区块 1 右边界 → 转向向右。
4. 过程中若检测到左侧怪物距离 80px，则立即向左追踪并攻击，直到怪物死亡或离开检测框，然后恢复巡逻（此时可能处于任意位置，按当前朝向继续巡逻）。

---

此文档可作为开发实现的标准需求，如有歧义请及时沟通细化。